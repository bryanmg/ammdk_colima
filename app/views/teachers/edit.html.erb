<h1 class="subtitle" >Editing my profile</h1>

<%= render 'form', new_user: @user, user: @user, url: teacher_url(@user), method: :patch %>

<br>

<div>
  <%= link_to "Show my profile", teacher_url(@user) %> |
  <%= link_to "Back to dashboard", teacher_url(@user) %>
</div>
