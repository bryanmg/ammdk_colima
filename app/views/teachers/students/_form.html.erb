<%= form_with(model: student, url: url, method: method) do |form| %>

  <% if student.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(student.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
        <% student.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>

    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name, value: student.name, required: true %>
  </div>

  <div>
    <%= form.label :email, style: "display: block" %>
    <%= form.email_field :email, value: student.email %>
  </div>

  <div>
    <%= form.label :belt, style: "display: block" %>
    <%= form.select :belt, BELTS.keys, value: student.belt%>
  </div>

  <div>
    <%= form.label :birth_date, style: "display: block" %>
    <%= form.date_field :birth_date, value: student.birth_date %>
  </div>

  <div>
    <%= form.label :group, style: "display: block" %>
    <%= form.select :group, current_user.groups.all.pluck(:name, :id) %>
  </div>

  <%= form.fields_for :student_information do |field| %>
    <div>
      <%= field.label :ocupation, style: "display: block" %>
      <%= field.text_field :ocupation, value: student.student_information&.ocupation%>
    </div>

    <div>
      <%= field.label :civil_status, style: "display: block" %>
      <%= field.text_field :civil_status, value: student.student_information&.civil_status %>
    </div>

    <span class="subtitle">Tutor information</span>

    <div>
      <%= field.label :tutor_name, style: "display: block" %>
      <%= field.text_field :tutor_name, value: student.student_information&.tutor_name %>
    </div>

    <div>
      <%= field.label :cellphone, style: "display: block" %>
      <%= field.text_field :cellphone, value: student.student_information&.cellphone %>
    </div>

    <div>
      <%= field.label :health_insurance, style: "display: block" %>
      <%= field.text_field :health_insurance, value: student.student_information&.health_insurance %>
    </div>
  
  <% end %>
  <br/>

  <div>
    <%= form.submit %>
  </div>
<% end %>
